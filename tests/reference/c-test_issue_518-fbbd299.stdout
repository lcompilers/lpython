#include <assert.h>
#include <complex.h>
#include <inttypes.h>
#include <stdio.h>
#include <math.h>

#include <lfortran_intrinsics.h>

void _lpython_main_program()
{
    main0();
    main();
}

long long fib(int64_t n)
{
    int64_t _lpython_return_variable;
    if (n < 2) {
        _lpython_return_variable = n;
        return _lpython_return_variable;
    } else {
        _lpython_return_variable = fib(n - 1) + fib(n - 2);
        return _lpython_return_variable;
    }
    return _lpython_return_variable;
}

void _xx_lpython_changed_main_xx()
{
    int64_t ans;
    ans = fib(10);
    assert (ans == 55);
}

void main0()
{
    int64_t ans;
    ans = fib(15);
    assert (ans == 610);
}

int main(int argc, char* argv[])
{
    _lpython_main_program();
    return 0;
}
