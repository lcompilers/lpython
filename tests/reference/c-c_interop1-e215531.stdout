#include <assert.h>
#include <complex.h>
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <stdbool.h>

#include <lfortran_intrinsics.h>

// Forward declarations
double f(double x);
void g(double a, float b, int64_t c, int32_t d);
double h(double x);
void l(double a, float b, int64_t c, int32_t d);
void main0();

// Implementations
double f(double x);

void g(double a, float b, int64_t c, int32_t d);

double h(double x)
{
    double _lpython_return_variable;
    _lpython_return_variable = x + 1.000000;
    return _lpython_return_variable;
}

void l(double a, float b, int64_t c, int32_t d)
{
    printf("%s\n", "OK");
}

void main0()
{
    double i;
    double x;
    float y;
    int64_t z;
    int32_t zz;
    x = 5.000000;
    i = f(x);
    y = 5.400000;
    z = 3;
    zz = 2;
    g(x, y, z, zz);
    i = h(x);
    l(x, y, z, zz);
}

int main(int argc, char* argv[])
{
    return 0;
}
