#include <assert.h>
#include <complex.h>
#include <inttypes.h>
#include <stdio.h>

#include <lfortran_intrinsics.h>

double f(double x);

void g(double a, float b, int64_t c, int32_t d);

void main0()
{
    double i;
    double x;
    float y;
    int64_t z;
    int32_t zz;
    x = 5.000000;
    i = f(x);
    y = 5.400000;
    z = 3;
    zz = 2;
    g(x, y, z, zz);
}

int main(int argc, char* argv[])
{
    return 0;
}
